# Function - Power of Number (Base raised to exponent)

def power(base, exponent):
    return base ** exponent

base = int(input('Enter Base: '))
exponent = int(input('Enter Power: '))

print(power(base, exponent))
