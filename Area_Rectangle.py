# Area of a Rectangle

length = float(input("Enter the Length: "))
width = float(input("Enter the Width: "))

area = length * width
print(f"Area of Rectangle of Length {length} & Width {width} is: {area}")
